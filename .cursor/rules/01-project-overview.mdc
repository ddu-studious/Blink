---
alwaysApply: true
---
# QingMou (青眸) - 项目概览

## 项目信息

- **项目名称**: QingMou / 青眸 (护眼桌面应用)
- **版本**: v1.0.0
- **创建日期**: 2026
- **项目类型**: Electron 跨平台桌面应用

## 项目简介

本项目是一个**跨平台护眼桌面应用**，基于 20-20-20 法则，通过智能定时提醒、全屏休息覆盖和使用统计帮助用户保护视力。主要包括：

- **定时提醒服务**：双级休息机制 (Mini Break 20秒 / Long Break 5分钟)
- **全屏休息覆盖**：美观的全屏遮罩，支持多显示器
- **智能场景感知**：空闲检测、休眠感知、免打扰模式感知
- **使用统计**：休息次数/时长/达成率的可视化统计
- **系统托盘**：常驻系统托盘，低调运行

## 核心功能模块

### 主进程 (Main Process)
- **TimerManager**: 核心计时逻辑，管理 Mini/Long Break 周期
- **TrayManager**: 系统托盘图标和菜单
- **OverlayManager**: 全屏休息窗口管理 (支持多屏)
- **PowerMonitor**: 休眠/唤醒/锁屏事件监听
- **IdleDetector**: 用户空闲状态检测
- **SettingsStore**: 用户偏好持久化
- **StatsDatabase**: 统计数据存储 (SQLite)

### 渲染进程 (Renderer Process)
- **Rest Page**: 休息倒计时页面 (Mini Break / Long Break)
- **Settings Page**: 偏好设置页面
- **Dashboard Page**: 统计仪表盘
- **Welcome Page**: 首次启动引导

## 技术栈

### 核心框架
- **桌面框架**: Electron (latest LTS)
- **构建工具**: electron-vite
- **前端框架**: React 18 + TypeScript 5
- **CSS 方案**: Tailwind CSS 3 + Shadcn/ui
- **状态管理**: Zustand
- **打包工具**: electron-builder

### 功能依赖
- **数据存储**: electron-store (设置) + better-sqlite3 (统计)
- **日志**: electron-log
- **自启动**: auto-launch
- **国际化**: i18next
- **自动更新**: electron-updater
- **图表**: Recharts
- **动画**: Framer Motion
- **图标**: Lucide React
- **时间**: dayjs

### 系统 API
- `Tray` - 系统托盘
- `BrowserWindow` - 窗口管理
- `powerMonitor` - 电源/空闲监测
- `screen` - 多屏管理
- `Notification` - 系统通知
- `globalShortcut` - 全局快捷键
- `nativeTheme` - 主题切换

### 开发工具
- **测试**: Vitest + Playwright
- **代码规范**: ESLint + Prettier
- **Git Hooks**: Husky + lint-staged

## 目录结构

```
qingmou/
├── .cursor/                          # Cursor AI 配置
│   ├── rules/                        # 开发规则 (MDC 格式)
│   │   ├── 01-project-overview.mdc   # 项目概览
│   │   └── 02-documentation.mdc      # 文档管理规范
│   └── commands/                     # 命令脚本
│       ├── important-context.md      # 项目上下文
│       └── mcpcommands.md            # MCP 命令快捷方式
├── docs/                             # 项目文档
│   ├── README.md                     # 文档索引
│   ├── architecture/                 # 架构文档
│   ├── requirements/                 # 需求文档
│   ├── research/                     # 调研文档
│   ├── guides/                       # 使用指南
│   ├── api/                          # API 文档
│   └── changelog/                    # 变更日志
├── resources/                        # 应用资源
│   ├── icons/                        # 应用图标
│   ├── sounds/                       # 提示音
│   └── images/                       # 图片资源
├── src/
│   ├── main/                         # 主进程
│   │   ├── index.ts                  # 主进程入口
│   │   ├── timer/                    # 计时器模块
│   │   ├── tray/                     # 系统托盘模块
│   │   ├── window/                   # 窗口管理模块
│   │   ├── monitor/                  # 系统监控模块
│   │   ├── store/                    # 数据存储模块
│   │   ├── updater/                  # 自动更新模块
│   │   └── ipc/                      # IPC 通信
│   ├── preload/                      # 预加载脚本
│   │   └── index.ts                  # 安全 API 桥接
│   └── renderer/                     # 渲染进程 (React)
│       ├── src/
│       │   ├── pages/                # 页面组件
│       │   ├── components/           # 通用组件
│       │   ├── hooks/                # 自定义 Hooks
│       │   ├── stores/               # Zustand 状态
│       │   ├── utils/                # 工具函数
│       │   ├── i18n/                 # 国际化资源
│       │   └── styles/               # 全局样式
│       └── index.html
├── electron.vite.config.ts           # electron-vite 配置
├── electron-builder.yml              # 打包配置
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── README.md
```

## 核心模块说明

| 模块 | 位置 | 说明 |
|------|------|------|
| TimerManager | `src/main/timer/` | 计时核心，管理 Mini/Long Break 周期 |
| TrayManager | `src/main/tray/` | 系统托盘图标、菜单、倒计时显示 |
| OverlayManager | `src/main/window/` | 全屏覆盖窗口，支持多屏 |
| PowerMonitor | `src/main/monitor/` | 休眠/唤醒/锁屏事件处理 |
| IdleDetector | `src/main/monitor/` | 用户空闲状态检测 |
| SettingsStore | `src/main/store/` | electron-store 用户设置 |
| StatsDatabase | `src/main/store/` | better-sqlite3 统计数据 |
| IPC Handlers | `src/main/ipc/` | 主进程↔渲染进程通信 |
| Rest Page | `src/renderer/src/pages/Rest/` | 休息倒计时 (React) |
| Settings Page | `src/renderer/src/pages/Settings/` | 偏好设置 (React) |
| Dashboard | `src/renderer/src/pages/Dashboard/` | 统计仪表盘 (React + Recharts) |

## 数据存储

### 设置数据 (electron-store / JSON)
- 存储路径: `app.getPath('userData')/settings.json`
- 包含: 提醒间隔、休息时长、主题、语言、快捷键等

### 统计数据 (SQLite)
- 存储路径: `app.getPath('userData')/stats.db`
- 核心表: `break_records` (休息记录)、`daily_stats` (每日统计)

## 进程架构

```
Main Process (Node.js)
├── TimerManager ←→ PowerMonitor / IdleDetector
├── TrayManager
├── OverlayManager → 创建 BrowserWindow (多屏)
├── SettingsStore (electron-store)
├── StatsDatabase (better-sqlite3)
└── IPC Bridge
     ↕ contextBridge
Renderer Process (Chromium)
├── Rest Overlay (React)
├── Settings Window (React)
└── Dashboard Window (React)
```

## 重要提示

1. **文档管理**: 所有文档必须放在 `docs/` 目录下
2. **安全规范**: 渲染进程必须启用 `contextIsolation: true` 和 `sandbox: true`
3. **IPC 通信**: 所有进程间通信通过 `contextBridge` + `ipcMain/ipcRenderer`
4. **多屏支持**: 使用 `screen.getAllDisplays()` 管理多显示器
5. **后台运行**: macOS 设置 `LSUIElement: 1`，不显示 Dock 图标
6. **资源管理**: 休息窗口按需创建，关闭后销毁，减少内存占用
7. **数据本地化**: 所有用户数据仅存本地，不上传服务器

---

**最后更新**: 2026-02-13
